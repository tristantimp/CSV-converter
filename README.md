# CSV-converter
Allows you to convert CSV files to LST files. Useful for WeBWork. 

# Steps
First, download the csv_converter.py file. 
After opening the program, select the .CSV file you want to convert. A .LST file will be automatically created with the same name as the .CSV file when you click "Convert". 
